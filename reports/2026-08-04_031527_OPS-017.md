# OPS-017 Stopped Acceptance Report

## Task and timing

- Task: OPS-017 — Add bounded OPS-015 launcher and manual smoke setup
- Branch: `agent/ops-017-telegram-smoke-launcher`
- Base: `d8dbe310e9d4137447e2b116c9b5d62686f61e20`
- Governance commit: `ea598d0`
- Automated implementation commit: `46af146`
- Task start: 2026-08-04 02:55:00 UTC
- Task end: 2026-08-04 03:16:01 UTC
- Elapsed: 21 minutes 1 second
- Continuity: resumed after Josh approved the revised proposal
- Stale status: not stale
- Blocked status: blocked at separately approved external-action gate
- Approval needed: create `/etc/trading-bot/ops-015.env` and perform real
  Telegram I/O; neither action has occurred

## Files changed

- `docs/proposals/OPS-017.md`
- `AGENT_BACKLOG.md`
- `engineering/telegram_service.py`
- `engineering/telegram_adapter.py`
- `tests/test_engineering_telegram_service.py`
- `tests/test_engineering_telegram_adapter.py`
- `MENTOR.md`
- `TRADING_BOT_AUTONOMOUS_ENGINEERING_HANDOFF.md`
- `ITERATION_PROGRESS_LOG.md`
- `reports/2026-08-04_031527_OPS-017.md`
- ignored rolling `REPORT.md`

No external secret, real token, Telegram request, smoke database, normal event
database, systemd/service, dashboard, approval, CONFIG-001, trading, brokerage,
OpenClaw, deployment, push, or PR state was created or modified.

## Automated test evidence

Focused command:

```text
TESTING=1 UNIT_TESTING=1 .venv/bin/python -m pytest tests/test_engineering_telegram_service.py tests/test_engineering_telegram_adapter.py tests/test_engineering_telegram_transport.py tests/test_engineering_control.py tests/test_engineering_event_store.py tests/test_engineering_query_service.py -q
```

Result: `67 passed, 1 warning in 1.75s`.

Full safe command:

```text
TESTING=1 UNIT_TESTING=1 .venv/bin/python -m pytest -q
```

Result: `317 passed, 2 warnings in 21.53s`; paper defaults active and live
brokerage calls blocked. All Telegram boundaries were fake/replaced. No
backtest applies.

## Acceptance evidence

### Criterion 1

Original criterion: Runtime configuration uses exactly the two approved names;
real values live only in the external operator-owned 0600 file and are never
disclosed or persisted.

- Proof method: secret parser tests cover exact keys, syntax, size, symlink,
  mode, owner, malformed values, environment nonmutation, and canary-safe errors.
- Exact result: automated contract passes, but the real external file does not
  exist because Josh has not approved its creation.
- Status: FAIL

### Criterion 2

Original criterion: The repository launcher exposes the exact approved command
and validates external file, repository, both bounds, fixed origin, and exact
isolated smoke store before network access.

- Proof method: launcher source inspection, exact argument/path rejection tests,
  and fake boundary canaries.
- Exact result: exact 20-poll/300-second command and isolated path are enforced;
  invalid configurations exit 2 before transport/store construction.
- Status: PASS

### Criterion 3

Original criterion: Smoke mode is foreground-only, stops at 20 polls or 300
seconds whichever occurs first, cleans up signals, and has no daemon, endless
polling, restart, or service behavior.

- Proof method: max-poll, max-time, signal, static capability, and no-extra-poll
  tests with fake clocks/signals.
- Exact result: poll 21 never occurs; the earlier time bound stops polling;
  SIGINT/SIGTERM map to 130/143; no daemon/restart/service capability exists.
- Status: PASS

### Criterion 4

Original criterion: Before real smoke, verify Josh initiated the private chat
and chat/sender IDs equal the configured positive allowlist without displaying
the token or inbound content.

- Proof method: automated allowlist behavior already exists from OPS-015; real
  operator observation requires Telegram I/O.
- Exact result: not performed by explicit instruction.
- Status: FAIL

### Criterion 5

Original criterion: Josh receives exact bounded `/status`, `/current`, and
`/report` responses with no raw/unbounded disclosure.

- Proof method: automated OPS-015 fake-query coverage plus required real smoke.
- Exact result: automated behavior remains green; real responses not observed.
- Status: FAIL

### Criterion 6

Original criterion: An unauthorized second account receives no response and one
sanitized denial in the isolated smoke store; normal event DB is untouched.

- Proof method: automated denial/isolation tests plus required real smoke.
- Exact result: fake-boundary denial and normal-DB mtime/content canaries pass;
  real unauthorized attempt not performed.
- Status: FAIL

### Criterion 7

Original criterion: Pause/repeated-pause/resume are audited in the isolated
store and unconditional cleanup restores pre-smoke pause state on every terminal
path without touching the normal event DB.

- Proof method: automated success, failure, SIGINT, SIGTERM, deadline,
  competing-poller, max-polls, max-seconds, and cleanup-failure tests.
- Exact result: automated restoration and isolation pass on every path; real
  command/audit sequence not performed.
- Status: FAIL

### Criterion 8

Original criterion: Launcher emits bounded allowlisted JSON stderr logs for the
runtime lifecycle without tokens, identities, content, raw payloads, URLs, or
arbitrary exceptions.

- Proof method: structured-log schema, safe-command, unknown-field, canary, and
  logging-failure tests.
- Exact result: one-line JSON uses fixed fields/reason codes; unsafe fields and
  commands are rejected; logging failure exits 5 after cleanup.
- Status: PASS

### Criterion 9

Original criterion: Deterministic exits distinguish competing poller 3,
permanent Telegram 4, config 2, runtime 5, success 0, SIGINT 130, and SIGTERM
143 while transient retries remain bounded.

- Proof method: parametrized outcome and transient retry tests.
- Exact result: every listed outcome passed with fake transport, clock, signal,
  and sleeper boundaries.
- Status: PASS

### Criterion 10

Original criterion: Automated fake-boundary suites pass, then a separately
approved real seven-interaction smoke and cleanup complete.

- Proof method: exact focused and full suite commands above; external phase
  intentionally withheld.
- Exact result: focused 67 and full 317 pass; real sequence not approved or run.
- Status: FAIL

## Exact proposed external setup

Do not execute without Josh approval:

```text
sudo install -d -m 0700 /etc/trading-bot
sudo install -m 0600 /dev/null /etc/trading-bot/ops-015.env
sudoedit /etc/trading-bot/ops-015.env
```

Enter exactly two lines interactively, replacing placeholders locally:

```text
ENGINEERING_TELEGRAM_BOT_TOKEN=<BotFather token>
ENGINEERING_TELEGRAM_JOSH_CHAT_ID=<Josh positive numeric private chat ID>
```

Never paste either value into chat, shell history, Git, reports, or logs.

## Exact proposed smoke command

Run from the repository root only after approval and after confirming no other
`getUpdates` consumer is active:

```text
.venv/bin/python -m engineering.telegram_service --env-file /etc/trading-bot/ops-015.env --smoke --event-store .agent-state/telegram-smoke-events.sqlite3 --max-polls 20 --max-seconds 300
```

During the foreground run, exercise exactly: Josh `/status`, `/current`,
`/report`, `/pause`, repeated `/pause`, `/resume`, then unauthorized `/status`.

## Risks and cleanup

- Polling consumes updates; a competing consumer must be absent.
- The token and identities must remain local and undisclosed.
- Cleanup always attempts lease release and isolated pause restoration; hard
  kill falls back to finite lease expiry.
- Preserve the isolated smoke DB as audit evidence; do not delete it without
  Josh's approval.
- If the token is exposed, revoke/rotate it through verified BotFather.
- Code rollback remains branch based; no PR exists yet.

## Manager decision and next action

BLOCKED correctly. Josh must review and explicitly approve or reject the exact
external setup and smoke command. Do not push or create a PR yet, and do not
begin another task.
